{
  "name": "chat-worker",
  "version": "1.0.0",
  "description": "Durable Objects Chat WebSocket Server for VLBMX",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate --schema src/db/schema.ts --out src/db/migrations --dialect sqlite && node generate-migrations.js"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "drizzle-kit": "^0.31.9",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
